{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Project Plan Schema",
  "type": "object",
  "required": ["metadata", "epics"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "metadata": {
      "type": "object",
      "required": ["project", "version", "workItemTypes"],
      "additionalProperties": false,
      "properties": {
        "project": { "type": "string", "minLength": 1 },
        "version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+$" },
        "description": { "type": "string" },
        "assignmentStrategy": { "type": "string", "enum": ["feature-owner"] },
        "workItemTypes": {
          "type": "object",
          "description": "Azure DevOps work item type names. Must match your project's process template: Agile (Epic/Feature/User Story), Scrum (Epic/Feature/Product Backlog Item), Basic (Epic/Issue/Task), CMMI (Epic/Feature/Requirement).",
          "required": ["epic", "feature", "task"],
          "additionalProperties": false,
          "properties": {
            "epic": {
              "type": "string",
              "minLength": 1,
              "description": "Level-1 work item type. Usually 'Epic' across all process templates.",
              "examples": ["Epic"]
            },
            "feature": {
              "type": "string",
              "minLength": 1,
              "description": "Level-2 work item type. Varies by process: 'Feature' (Agile/Scrum/CMMI), 'Issue' (Basic).",
              "examples": ["Feature", "Issue"]
            },
            "task": {
              "type": "string",
              "minLength": 1,
              "description": "Level-3 work item type. Varies by process: 'Task' (Agile/Basic), 'Product Backlog Item' (Scrum), 'Requirement' (CMMI), 'User Story' (Agile).",
              "examples": ["Task", "Product Backlog Item", "User Story", "Requirement"]
            }
          }
        }
      }
    },
    "epics": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/epic" }
    }
  },
  "$defs": {
    "epic": {
      "type": "object",
      "required": ["id", "title", "features"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^[A-Z0-9-]+$" },
        "title": { "type": "string", "minLength": 1 },
        "description": { "type": "string" },
        "ownerUserIds": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "fields": { "$ref": "#/$defs/customFields" },
        "features": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/feature" }
        }
      }
    },
    "feature": {
      "type": "object",
      "required": ["id", "title", "ownerUserIds", "tasks"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^[A-Z0-9-]+$" },
        "title": { "type": "string", "minLength": 1 },
        "description": { "type": "string" },
        "ownerUserIds": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "fields": { "$ref": "#/$defs/customFields" },
        "tasks": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/task" }
        }
      }
    },
    "task": {
      "type": "object",
      "required": ["id", "title"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^[A-Z0-9-]+$" },
        "title": { "type": "string", "minLength": 1 },
        "description": { "type": "string" },
        "assignedTo": { "type": "string", "minLength": 1 },
        "ownerUserIds": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "fields": { "$ref": "#/$defs/customFields" }
      }
    },
    "customFields": {
      "type": "object",
      "additionalProperties": {
        "oneOf": [
          { "type": "string" },
          { "type": "number" },
          { "type": "boolean" }
        ]
      }
    }
  }
}
